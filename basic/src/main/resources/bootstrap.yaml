spring:
  application:
    name: profile-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8082
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/realm